---
import { getCollection } from "astro:content";
import Button from "../button.astro";
import Project from "../project.astro";

let projects = await getCollection("project");
projects = projects.sort((a, b) => {
  return new Date(b.data.date).getTime() - new Date(a.data.date).getTime();
});
---

<section class="max-w-4xl mx-auto px-7 lg:px-0">
  <h2
    class="text-2xl font-bold leading-10 tracking-tight text-neutral-900 dark:text-neutral-100"
  >
    My Projects
  </h2>
  <p class="mb-6 text-base text-neutral-600 dark:text-neutral-400">
    These are the main projects I’ve contributed to, including company products I’ve helped build and personal or academic projects that reflect my interests in design and technology.
  </p>
  <div
    class="grid items-stretch w-full grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-7 mt-7"
  >
    {
      projects.map((project) => (
        <Project
          name={project.data.title}
          description={project.data.description}
          image={project.data.cardImage || project.data.screenshots?.[0]}
          slug={project.slug}
        />
      ))
    }
  </div>

  <div class="flex items-center justify-center w-full py-5">
    <Button text="View All My Projects" link="/projects" />
  </div>
</section>
